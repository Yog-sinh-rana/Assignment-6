// 9. Write a program to calculate LCM of two numbers
#include<stdio.h>
 int main() {
 
     int a,b,i;
     printf("Enter A Number:");
     scanf("%d%d",&a,&b);
     for ( i = i; i<=a*b; i++)
     if((i%a==0) && (i%b==0))
     {
        break;
     }
     printf("%d is LCM\t",i);

     return 0;
     
 }
     
